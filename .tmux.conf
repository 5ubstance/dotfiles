# change prefix to Ctrl-a (like in gnu-screen)
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# shell
set -g default-command /bin/zsh
set -g default-shell /bin/zsh

# start with window 1 (instead of 0)
set -g base-index 1

# start with pane 1
set -g pane-base-index 1

# screen mode
#set -g default-terminal "screen-256color"

# source config file
bind r source-file ~/.tmux.conf

# history
set -g history-limit 10000

# allow terminal scrolling
set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'

# vi copy mode
setw -g mode-keys vi

# map proper symbols to horizontal/vertical tabbing
unbind %
bind | split-window -h
bind - split-window -v

# Set status bar
#set -g status-bg default
#set -g status-fg white
#set -g status-left '#[fg=white]#H'
#set-window-option -g window-status-current-bg blue
#set-window-option -g window-status-current-fg white
#set-window-option -g window-status-bg black
#set-window-option -g window-status-fg white

# Set window notifications
setw -g monitor-activity on
set -g visual-activity on
set -g visual-content on
set -g visual-bell off
set -g bell-action any
set -g bell-on-alert on

# Environment bootstrap
#new-session -d -s corestation
#new-window -t corestation:1 -n 'localhost' 'ssh root@localhost'
#new-window -t corestation:2 -n 'vpn1' 'ssh root@vpn1'
#new-window -t corestation:3 -n 'impact3' 'ssh root@impact3'
#select-window -t corestation:1

# Powerline statusline
#source '~/.local/lib/python2.7/site-packages/powerline/bindings/tmux/powerline.conf'
set -g status on
set -g status-utf8 on
set -g status-interval 2
set -g status-fg colour231
set -g status-bg colour234
#set -g status-bg transparent
set -g status-left-length 20
set -g status-left '#[fg=colour16,bg=colour254,bold] #S #[fg=colour254,bg=colour234,nobold]#(eval $POWERLINE_COMMAND tmux left)'
#set -g status-left '#[fg=colour16,bg=colour254,bold] #S #[fg=colour254,bg=transparent,nobold]#(eval $POWERLINE_COMMAND tmux left)'
set -g status-right '#(eval $POWERLINE_COMMAND tmux right -R pane_id=`tmux display -p "#D"`)'
set -g status-right-length 150
set -g window-status-format "#[fg=colour244,bg=colour234]#I #[fg=colour240] #[fg=colour249]#W "
#set -g window-status-format "#[fg=colour244,bg=transparent]#I #[fg=colour240] #[fg=colour249]#W "
set -g window-status-current-format "#[fg=colour234,bg=colour31]#[fg=colour117,bg=colour31] #I  #[fg=colour231,bold]#W #[fg=colour31,bg=colour234,nobold]"
#set -g window-status-current-format "#[fg=transparent,bg=colour31]#[fg=colour117,bg=colour31] #I  #[fg=colour231,bold]#W #[fg=colour31,bg=transparent,nobold]"
# vim: ft=tmux"
